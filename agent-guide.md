# ML Training Workflow Agents Guide

이 문서는 자율주행 ML 학습 워크플로우를 자동화하는 6개의 Claude subagent 사용법을 설명합니다.

---

## 개요

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                        ML TRAINING WORKFLOW                                      │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│   1. PLAN          2. TRAIN          3. ANALYZE         4. DECIDE              │
│   ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐             │
│   │ planner  │ ──▶ │ monitor  │ ──▶ │ analyst  │ ──▶ │orchestrator│            │
│   └──────────┘     └──────────┘     └──────────┘     └──────────┘             │
│        │                                                   │                   │
│        └───────────────────────────────────────────────────┘                   │
│                                                                                 │
│   Support Agents:                                                               │
│   ┌───────────────┐    ┌────────────────────┐                                  │
│   │ doc-manager   │    │ site-publisher     │                                  │
│   │ (문서 동기화)  │    │ (gh-pages 발행)    │                                  │
│   └───────────────┘    └────────────────────┘                                  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## Agent 목록

| Agent | 역할 | 위치 |
|-------|------|------|
| `training-planner` | 실험 설계, Config 생성 | `.claude/agents/training-planner.md` |
| `training-monitor` | 학습 상태 모니터링 | `.claude/agents/training-monitor.md` |
| `training-analyst` | 결과 분석, 보고서 생성 | `.claude/agents/training-analyst.md` |
| `training-doc-manager` | 문서 동기화, 아카이브 | `.claude/agents/training-doc-manager.md` |
| `training-site-publisher` | gh-pages 사이트 발행 | `.claude/agents/training-site-publisher.md` |
| `training-orchestrator` | 전체 워크플로우 조율 | `.claude/agents/training-orchestrator.md` |

---

## 1. training-planner

### 역할
새로운 ML 실험을 설계하고 학습 Config를 생성합니다.

### 트리거 키워드
`실험 설계`, `다음 버전`, `config 생성`, `Phase 계획`, `새 실험`, `v11`, `v12`, `phaseA`

### 사용 예시
```
"v11 실험 설계해줘"
"Phase H config 만들어줘"
"다음 버전 계획해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ | `docs/TRAINING-LOG.md`, `docs/LEARNING-ROADMAP.md`, `python/configs/planning/*.yaml` |
| WRITE | `python/configs/planning/*.yaml`, `experiments/v12_phaseX/README.md` |

### 출력물
- 새 YAML config 파일
- 실험 README 문서
- 가설 및 예상 결과 정리

---

## 2. training-monitor

### 역할
실행 중인 학습의 상태를 모니터링하고 진행률을 보고합니다.

### 트리거 키워드
`학습 상태`, `진행률`, `모니터링`, `현재 reward`, `몇 스텝`, `training status`, `check training`

### 사용 예시
```
"현재 학습 진행률 확인해줘"
"v10g 몇 스텝이야?"
"학습 상태 체크해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ | `results/{run-id}/run_logs/`, `results/{run-id}/E2EDrivingAgent/` |
| WRITE | `.claude/analytics/metrics.jsonl` |

### 출력물
- 현재 스텝, reward, 진행률
- Curriculum 상태
- 이상 감지 알림

---

## 3. training-analyst

### 역할
학습 결과를 분석하고 성공/실패를 판정하며 원인을 분석합니다.

### 트리거 키워드
`결과 분석`, `리포트`, `왜 실패`, `원인 분석`, `학습 완료`, `분석해줘`, `보고서`

### 사용 예시
```
"v10g 결과 분석해줘"
"왜 실패했는지 분석해줘"
"학습 완료됐어, 분석해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ | `results/{run-id}/`, `.claude/analytics/`, `docs/TRAINING-LOG.md` |
| WRITE | `docs/TRAINING-LOG.md`, `experiments/v12_phaseX/README.md` |

### 출력물
- 성공/실패 판정
- Root cause 분석
- 권장 조치 사항
- TRAINING-LOG.md 결과 섹션

### 판정 기준
| 판정 | 조건 |
|------|------|
| ✅ 성공 | Final Reward > Target × 90% |
| 🟡 부분 성공 | Final Reward > Target × 70% |
| 🔴 실패 | Final Reward < Target × 70% |
| ⚫ 발산 | Reward 지속 하락 |

---

## 4. training-doc-manager

### 역할
프로젝트 문서들을 동기화하고 아카이브를 관리합니다.

### 트리거 키워드
`문서 동기화`, `아카이브`, `진행 업데이트`, `로그 정리`, `문서 업데이트`, `sync docs`

### 사용 예시
```
"문서 동기화해줘"
"TRAINING-LOG 업데이트해줘"
"완료된 로그 아카이브해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ | `docs/*`, `experiments/*/README.md` |
| WRITE | `docs/TRAINING-LOG.md`, `docs/PROGRESS.md`, `docs/LEARNING-ROADMAP.md`, `docs/archives/` |

### 출력물
- 동기화된 문서들
- 아카이브 파일 (`docs/archives/TRAINING-LOG-ARCHIVE-{date}.md`)
- 동기화 리포트

### 동기화 대상
```
TRAINING-LOG.md ←→ experiments/README.md
PROGRESS.md ←→ TRAINING-LOG.md
phases/README.md ←→ PROGRESS.md
```

---

## 5. training-site-publisher

### 역할
GitHub Pages 사이트 (physical-unity-site)를 업데이트합니다.

### 트리거 키워드
`사이트 업데이트`, `gh-pages`, `웹 발행`, `갤러리`, `publish`, `site update`

### 사용 예시
```
"Phase G 사이트 업데이트해줘"
"gh-pages에 반영해줘"
"갤러리에 스크린샷 추가해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ (Source) | `C:\Users\user\Desktop\dev\physical-unity\docs\*`, `experiments\*` |
| WRITE (Target) | `C:\Users\user\Desktop\dev\physical-unity-site\*` |

### 출력물
- 업데이트된 Phase 문서 (`phases/phase-x.md`)
- 메인 페이지 (`index.md`)
- 갤러리 이미지
- Git 커밋 및 푸시

### 사이트 구조
```
physical-unity-site/
├── index.md                 # 메인 페이지
├── phases/
│   ├── index.md            # Phase 목록
│   ├── phase-a.md ~ phase-l.md
│   └── failed-experiments.md
├── lessons-learned.md       # 교훈 요약
└── gallery/
    ├── screenshots/
    └── charts/
```

---

## 6. training-orchestrator

### 역할
전체 워크플로우를 조율하고 다음 단계를 결정합니다.

### 트리거 키워드
`다음 단계`, `워크플로우`, `전체 상태`, `뭐 해야 해`, `next step`, `what's next`

### 사용 예시
```
"다음 뭐 해야 해?"
"전체 상태 알려줘"
"워크플로우 정리해줘"
```

### 입출력 폴더
| 구분 | 경로 |
|------|------|
| READ | 모든 문서 (조율 목적) |
| WRITE | `docs/PROGRESS.md` |

### 출력물
- 전체 상태 리포트
- 다음 단계 권장 사항
- Agent 호출 계획

### 의사결정 매트릭스
| 결과 | 다음 단계 | 호출 Agent |
|------|----------|-----------|
| ✅ 성공 | 다음 Phase 진행 | planner |
| 🟡 부분 성공 | 연장 학습 or 다음 Phase | analyst → planner |
| 🔴 실패 | 원인 분석 → 재설계 | analyst → planner |
| ⚫ 발산 | 롤백 → 재설계 | analyst → planner |

---

## 워크플로우 시나리오

### 시나리오 1: 학습 완료 후 전체 프로세스

```bash
# 1. 결과 분석
User: "v10g 결과 분석해줘"
→ training-analyst 호출
→ 성공/실패 판정, 원인 분석

# 2. 문서 업데이트
User: "문서 업데이트해줘"
→ training-doc-manager 호출
→ TRAINING-LOG.md, PROGRESS.md 동기화

# 3. 사이트 발행
User: "사이트에 반영해줘"
→ training-site-publisher 호출
→ gh-pages 업데이트 및 배포

# 4. 다음 단계 결정
User: "다음 뭐 해야 해?"
→ training-orchestrator 호출
→ 다음 Phase 또는 재설계 제안

# 5. 새 실험 설계 (성공 시)
User: "v11 설계해줘"
→ training-planner 호출
→ 새 config 및 실험 문서 생성
```

### 시나리오 2: 학습 중 모니터링

```bash
# 진행 상황 확인
User: "현재 학습 상태 확인해줘"
→ training-monitor 호출
→ 스텝, reward, 진행률 보고

# 이상 감지 시
→ 자동 알림: "⚠️ Reward 급락 감지"
→ User: "왜 그런지 분석해줘"
→ training-analyst 호출
```

### 시나리오 3: 빠른 전체 정리

```bash
User: "다음 뭐 해야 해?"
→ training-orchestrator 호출
→ 전체 상태 파악 후 다음 단계 제안
→ 필요한 agent들을 순차적으로 호출
```

---

## 관련 문서

| 문서 | 경로 | 설명 |
|------|------|------|
| TRAINING-LOG.md | `docs/TRAINING-LOG.md` | 학습 기록 |
| PROGRESS.md | `docs/PROGRESS.md` | 진행 상황 |
| LEARNING-ROADMAP.md | `docs/LEARNING-ROADMAP.md` | 학습 로드맵 및 교훈 |
| Phase 스펙 | `docs/phases/phase-N/SPEC.md` | Phase별 요구사항 |
| 실험 문서 | `experiments/v12_phaseX/README.md` | 실험별 상세 |
| gh-pages 사이트 | `C:\Users\user\Desktop\dev\physical-unity-site\` | 외부 공개 문서 |

---

## 팁

1. **"다음 뭐 해야 해?"** 로 시작하면 orchestrator가 전체 상황을 파악하고 안내합니다.
2. 학습 완료 후 **분석 → 문서 → 사이트 → 계획** 순서로 진행하면 됩니다.
3. 각 agent는 독립적으로도 사용 가능합니다.
4. 문서 동기화는 주기적으로 실행하면 좋습니다 (500K 스텝 단위 권장).
