# 기술 설계서 템플릿

## 문서 구조

```markdown
# {기능명} 기술 설계서

## 메타데이터
- **작성자**: {담당자}
- **작성일**: {YYYY-MM-DD}
- **버전**: {v1.0}
- **관련 PRD**: [{PRD 링크}](../prd/{feature}-prd.md)
- **상태**: Draft | Review | Approved

---

## 1. 개요 (Overview)

### 1.1 목적
> 이 문서의 목적과 범위

### 1.2 용어 정의
| 용어 | 정의 |
|------|------|
| {Term} | {Definition} |

---

## 2. 아키텍처 (Architecture)

### 2.1 시스템 구조
```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Client    │────▶│   Server    │────▶│  Database   │
└─────────────┘     └─────────────┘     └─────────────┘
```

### 2.2 컴포넌트 설명
| 컴포넌트 | 역할 | 기술 스택 |
|---------|------|----------|
| {Name} | {Role} | {Tech} |

### 2.3 데이터 흐름
1. {Step 1}
2. {Step 2}
3. {Step 3}

---

## 3. 데이터 모델 (Data Model)

### 3.1 엔티티 정의

#### {EntityName}
| 필드 | 타입 | 설명 | 제약조건 |
|------|------|------|----------|
| id | UUID | 고유 식별자 | PK |
| {field} | {type} | {desc} | {constraint} |

### 3.2 관계도
```
User (1) ──── (N) Order
Order (1) ──── (N) OrderItem
```

---

## 4. API 설계 (API Design)

### 4.1 엔드포인트 목록
| Method | Endpoint | 설명 |
|--------|----------|------|
| POST | /api/v1/{resource} | 생성 |
| GET | /api/v1/{resource}/{id} | 조회 |

### 4.2 상세 명세

#### POST /api/v1/{resource}
**Request**
```json
{
  "field": "value"
}
```

**Response (201)**
```json
{
  "id": "uuid",
  "field": "value",
  "createdAt": "2025-01-01T00:00:00Z"
}
```

**Error Codes**
| 코드 | 설명 |
|------|------|
| 400 | 잘못된 요청 |
| 401 | 인증 실패 |

---

## 5. 구현 상세 (Implementation Details)

### 5.1 핵심 로직
```python
# 핵심 알고리즘/로직만 포함
# 전체 코드는 파일 경로로 참조: src/services/{service}.py
```

### 5.2 의존성
| 패키지 | 버전 | 용도 |
|--------|------|------|
| {package} | {version} | {purpose} |

---

## 6. 보안 고려사항 (Security Considerations)

- [ ] 인증/인가 처리
- [ ] 입력값 검증
- [ ] SQL Injection 방지
- [ ] XSS 방지
- [ ] 민감 정보 암호화

---

## 7. 테스트 전략 (Test Strategy)

### 7.1 단위 테스트
| 대상 | 테스트 케이스 | 상태 |
|------|-------------|------|
| {Function} | {Case} | - [ ] |

### 7.2 통합 테스트
| 시나리오 | 상태 |
|---------|------|
| {Scenario} | - [ ] |

---

## 8. 배포 계획 (Deployment Plan)

### 8.1 환경별 설정
| 환경 | 설정 |
|------|------|
| Dev | {config} |
| Staging | {config} |
| Prod | {config} |

### 8.2 마이그레이션
- [ ] DB 마이그레이션 스크립트
- [ ] 롤백 계획

---

## 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|----------|
| v1.0 | {날짜} | {작성자} | 최초 작성 |
```

## 작성 가이드라인

### 섹션별 권장 길이
- 아키텍처: 500-800자 + 다이어그램
- 데이터 모델: 엔티티당 300-500자
- API 설계: 엔드포인트당 400-600자
- 구현 상세: 500-800자 (핵심만)

### 다이어그램 도구
- ASCII Art (간단한 구조)
- Mermaid (복잡한 구조, 별도 파일)

### 코드 포함 규칙
- 핵심 로직만 인라인
- 전체 구현은 파일 경로로 참조
- 예시 코드는 최소화
