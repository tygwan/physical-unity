# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF line endings (critical for execution)
*.sh text eol=lf

# Markdown files
*.md text eol=lf

# JSON files
*.json text eol=lf

# Keep these as-is
*.png binary
*.jpg binary
*.gif binary
